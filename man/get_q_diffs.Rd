% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_q_diffs.R
\name{get_q_diffs}
\alias{get_q_diffs}
\title{Extract q-difference components}
\usage{
get_q_diffs(object, fleet_labels = NULL, time_values = NULL)
}
\arguments{
\item{object}{A fitted \code{jointCPUE} object.}

\item{fleet_labels}{Optional character vector of length \code{n_f} giving labels
for fleet ids \code{0:(n_f-1)}. If \code{NULL}, default labels are used.}

\item{time_values}{Optional vector of length \code{n_t} used for plotting or
reporting time labels. If \code{NULL}, \code{1:n_t} is used.}
}
\value{
A named list with elements \code{system}, \code{time}, and \code{spatial}.
Components that are switched off are returned as \code{NULL}.
}
\description{
Extracts fleet-specific systematic, temporal, and spatial q-difference
components from a fitted \code{jointCPUE} model.
}
\details{
Temporal deviations are centered exactly as in the C++ model:
each fleet-specific time series is centered by its mean over observed
time steps (\code{has_tf == 1}), and unobserved time steps are returned as \code{NA}.

Spatial deviations are projected from mesh vertices onto the extrapolation
grid stored in the fitted object.
}
