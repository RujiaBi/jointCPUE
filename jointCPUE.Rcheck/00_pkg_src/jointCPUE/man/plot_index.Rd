% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_index.R
\name{plot_index}
\alias{plot_index}
\title{Plot standardized CPUE index and CV}
\usage{
plot_index(index_df, time_values = NULL, time_positions = NULL)
}
\arguments{
\item{index_df}{A data.frame returned by \code{\link[=get_index]{get_index()}} containing at least
\code{time}, \code{index}, and \code{cv}.}

\item{time_values}{Optional vector of labels for the x-axis. Must have the
same length as the number of rows in \code{index_df}. This can be used to show
actual calendar years or year-month labels instead of the default index
values in \code{index_df$time}.}

\item{time_positions}{Optional numeric vector of x-axis positions. Must have
the same length as the number of rows in \code{index_df}. This can be used to
preserve gaps in time, for example when some calendar years are missing.}
}
\value{
A ggplot object.
}
\description{
Creates a two-panel plot showing the standardized CPUE index and its
coefficient of variation (CV) over time.
}
