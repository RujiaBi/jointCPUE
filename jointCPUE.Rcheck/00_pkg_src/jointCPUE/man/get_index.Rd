% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{get_index}
\alias{get_index}
\title{Get bias-corrected index and uncertainty from an jointCPUE fit}
\usage{
get_index(
  object,
  level = 0.95,
  inner.control = list(sparse = TRUE, lowrank = TRUE, trace = FALSE),
  silent = TRUE
)
}
\arguments{
\item{object}{An object of class \code{jointCPUE} returned by \code{\link[=jointCPUE]{jointCPUE()}}.}

\item{level}{Confidence level for intervals. Default 0.95.}

\item{inner.control}{List passed to TMB::MakeADFun(inner.control=...).
Default uses sparse + lowrank for memory efficiency.}

\item{silent}{Logical; passed to TMB::MakeADFun() for the bias-correction step.}
}
\value{
A data.frame with columns:
\itemize{
\item time: 1:n_t
\item index: bias-corrected index (original scale)
\item log_index: log(index)
\item cv: SE on log scale (from sdreport for ADREPORT(link_total))
\item lwr, upr: CI on original scale using lognormal approximation
}
}
\description{
Computes a bias-corrected index on the original scale using the "epsilon trick"
(via eps_index) and returns log-scale SE from sdreport (ADREPORT(link_total)).
}
