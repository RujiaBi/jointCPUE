% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_data.R
\name{make_data}
\alias{make_data}
\title{Prepare data objects and mesh for jointCPUE workflows}
\usage{
make_data(
  data_utm,
  mesh,
  area_scale = "auto",
  index = c("monthly", "yearly"),
  month_col = NULL,
  month_diffs = c("on", "off")
)
}
\arguments{
\item{data_utm}{A data.frame containing required columns (with utm_x/y_scale)}

\item{mesh}{jointCPUEmesh built from make_mesh(), or a custom mesh}

\item{area_scale}{Numeric or "auto". Scaling factor for area_km2.}

\item{index}{Either \code{"monthly"} or \code{"yearly"}.}

\item{month_col}{Optional column name for calendar month (\code{1}--\code{12}). Used
only when \code{index = "yearly"} and \code{month_diffs = "on"}. If \code{NULL}, looks for
\code{month} or \code{Month}.}

\item{month_diffs}{"on" or "off". Controls whether a calendar month fixed
effect is prepared when \code{index = "yearly"}. The month effect uses a
sum-to-zero constraint over observed month levels, enters the observation
model only, and is excluded from the standardized index. When
\code{index = "monthly"}, the month effect is always disabled.}
}
\value{
A list with elements \code{data}, \code{key}, \code{scales}, and \code{time}.
}
\description{
Main data-prep function for jointCPUE.
\itemize{
\item mesh/SPDE/A matrices
\item extrapolation key grid + areas
}
}
\author{
Rujia Bi \email{bikayla5@gmail.com}
}
