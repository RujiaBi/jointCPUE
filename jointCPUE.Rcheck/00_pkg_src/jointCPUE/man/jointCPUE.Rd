% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{jointCPUE}
\alias{jointCPUE}
\title{Fit an integrated spatiotemporal CPUE standardization model}
\usage{
jointCPUE(
  data_utm,
  mesh,
  pop_spatial = c("on", "off"),
  pop_spatiotemporal = c("on", "off"),
  pop_spatiotemporal_type = c("iid", "rw"),
  q_diffs_system = c("on", "off"),
  q_diffs_time = c("on", "off"),
  q_diffs_spatial = c("on", "off"),
  control = list(eval.max = 1e+05, iter.max = 1e+05),
  ncores = NULL,
  ...,
  index = c("monthly", "yearly"),
  month_col = NULL,
  month_diffs = c("on", "off"),
  obs_sd = c("shared", "fleet"),
  extrapolation_grid = NULL,
  silent = FALSE
)
}
\arguments{
\item{data_utm}{A data.frame with required columns.}

\item{mesh}{An \code{jointCPUEmesh} or a bare fmesher mesh.}

\item{pop_spatial}{"on" or "off". If "off", the population-level
time-constant spatial field is excluded from both the likelihood and index.}

\item{pop_spatiotemporal}{"on" or "off". If "off", the population-level
spatiotemporal field is excluded from both the likelihood and index.}

\item{pop_spatiotemporal_type}{Either \code{"iid"} or \code{"rw"}. Controls the
temporal structure of the population-level spatiotemporal field when
\code{pop_spatiotemporal = "on"}. If \code{"iid"}, each time slice is independent.
If \code{"rw"}, the field follows a first-order random walk through time.}

\item{q_diffs_system}{"on" or "off". If "off", fleet effects are fixed to zero.}

\item{q_diffs_time}{"on" or "off". If "on", estimate fleet-specific temporal deviations.}

\item{q_diffs_spatial}{"on" or "off". If "on", estimate fleet-specific spatial deviations.}

\item{control}{Control list passed to \code{\link[stats:nlminb]{stats::nlminb()}}.}

\item{ncores}{Optional integer. If provided, sets the number of OpenMP threads. Passed to \code{\link[TMB:openmp]{TMB::openmp()}}.}

\item{...}{Passed to \code{\link[=make_data]{make_data()}} (for example \code{area_scale}).}

\item{index}{Either \code{"monthly"} or \code{"yearly"}. Chooses the time resolution of the
standardized index.}

\item{month_col}{Optional column name for calendar month (\code{1}--\code{12}). Used only
when \code{index = "yearly"} and \code{month_diffs = "on"}. If \code{NULL}, \code{jointCPUE()}
looks for \code{month} or \code{Month}.}

\item{month_diffs}{"on" or "off". Controls whether a calendar month fixed
effect is added when \code{index = "yearly"}. The month effect uses a sum-to-zero
constraint over observed month levels, enters the observation model only,
and is excluded from the standardized index. When \code{index = "monthly"}, the
month effect is always disabled regardless of this setting.}

\item{obs_sd}{Either \code{"shared"} or \code{"fleet"}. Controls the observation-error
standard deviation in the lognormal likelihood. \code{"shared"} uses a single
SD across all fleets. \code{"fleet"} estimates one SD per fleet.}

\item{extrapolation_grid}{Optional data.frame defining a common
extrapolation grid/key across fits. Use this when comparing joint and
single-fleet models on the same spatial integration domain. The supplied
object must contain \code{utm_x_scale} and \code{utm_y_scale}, plus either \code{area_km2}
or enough information to compute area (\code{lon}/\code{lat}, or \code{lon_std}/\code{lat}).}

\item{silent}{Logical. Passed to \code{\link[TMB:MakeADFun]{TMB::MakeADFun()}}.}
}
\value{
An object of class \code{jointCPUE} with elements \code{obj}, \code{opt}, \code{rep}, \code{prep}, etc.
}
\description{
Fits an integrated spatiotemporal model for CPUE standardization that jointly
models observation and sampling processes across one or more fisheries or surveys.
The framework is designed to reduce bias caused by preferential sampling,
targeting behavior, and heterogeneous effort distributions, and to estimate
a coherent latent abundance index.
}
\details{
The model is implemented using Template Model Builder (TMB). Spatial and
spatiotemporal random fields are represented using the SPDE (stochastic partial
differential equation) approach for computational efficiency. See the package
vignettes for worked examples and model-comparison workflows.
}
\author{
Rujia Bi \email{bikayla5@gmail.com}
}
